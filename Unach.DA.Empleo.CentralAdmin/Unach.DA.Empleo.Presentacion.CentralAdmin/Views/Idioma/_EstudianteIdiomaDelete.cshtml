@model EstudianteIdioma

<form role="form" id="form" method="post" asp-action="EstudianteIdiomaDelete" asp-controller="Idioma">

    <partial name="_ModalHeader" model="@(new  ModalHeader{ Heading = "Eliminar Idioma" })" />


    <div class="modal-body form-horizontal">

        @if (Model != null)
        {
            <input type="hidden" asp-for="Id" />

            @*@if (Model.Transaccion != null && Model.Transaccion.Count > 0)
        {
        <text>No se puede eliminar el módulo <strong>@Model.Nombre</strong>, debido a que contiene información relacionada.</text>
        }
        else
        {*@
            <text>Está seguro de eliminar la plantilla: <strong>@Model?.IdIdioma</strong>?</text>
            //}
        }
        else
        {
            <text>No existe información asociada.</text>
        }
    </div>

    <partial name="_ModalFooter" model="@(new ModalFooter { SubmitButtonText = "Eliminar", OnlyCancelButton = false, CancelButtonText = "Cancelar" })" />


</form>




