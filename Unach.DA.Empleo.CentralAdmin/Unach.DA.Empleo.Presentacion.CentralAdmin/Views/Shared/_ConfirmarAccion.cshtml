@model ConfirmarAccionViewModel

@*<partial name="_ModalHeader" model="@(new ModalHeader { Heading = @Model.Titulo  })" />*@

<div class="color-line"></div>
<div class="modal-header">
    @*<button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>*@
    <h5 class="modal-title" style="line-height:1em; height: 1em; overflow:hidden; cursor:help">@Model.Titulo</h5>
    <small class="font-bold"></small>
</div>

<div class="modal-body">
    <div>
        <div class="row">
            <div class="form-group  col-lg-12">
                <text>@Model.Advertencia</text>
            </div>
            @*@if (!Model)
                {
                    <div class="form-group  col-lg-12">
                        <text class="text-danger">Al momento no existen autoridades que firmen la presente acción de personal</text>
                    </div>
                }*@
        </div>
    </div>
</div>

<div class="modal-footer">
    @if (!Model.SoloBotonCancelar)
    {
        <button id="cancelarButton" data-dismiss="modal" class="no btn btn-default" type="button">No</button>
        <button id="confirmarButton" data-dismiss="modal" class="si btn btn-success ladda-button" type="button">Sí</button>
    }
    else
    {
        <button id="cancelarButton" data-dismiss="modal" class="no btn btn-default" type="button">Cancelar</button>
    }
</div>